🧩 شرح Modules في Terraform
ما هو Module؟
Module هو حاوية (Container) لمجموعة من الموارد (Resources) المرتبطة ببعضها، تُستخدم معاً لإنشاء بنية تحتية قابلة لإعادة الاستخدام.

text

┌─────────────────────────────────────────────────────────────┐
│                        Module                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  Variables  │  │  Resources  │  │   Outputs   │         │
│  │   (Input)   │──▶│   (Logic)   │──▶│  (Output)   │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
لماذا نستخدم Modules؟
text

┌────────────────────────────────────────────────────────────┐
│                    فوائد Modules                           │
├────────────────────────────────────────────────────────────┤
│  ✅ إعادة الاستخدام (Reusability)                          │
│  ✅ التنظيم (Organization)                                 │
│  ✅ التغليف (Encapsulation)                                │
│  ✅ الاتساق (Consistency)                                  │
│  ✅ سهولة الصيانة (Maintainability)                        │
└────────────────────────────────────────────────────────────┘
أنواع Modules
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   1️⃣ Root Module                                            │
│   ├── الموجود في المجلد الرئيسي                             │
│   └── نقطة البداية لـ Terraform                            │
│                                                             │
│   2️⃣ Child Module                                           │
│   ├── يتم استدعاؤه من module آخر                           │
│   └── يمكن أن يكون محلي أو خارجي                           │
│                                                             │
│   3️⃣ Published Module                                       │
│   ├── منشور على Terraform Registry                         │
│   └── جاهز للاستخدام مباشرة                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
هيكل Module الأساسي
text

📁 my-module/
├── 📄 main.tf          # الموارد الرئيسية
├── 📄 variables.tf     # المتغيرات المدخلة
├── 📄 outputs.tf       # المخرجات
├── 📄 providers.tf     # إعدادات المزودين (اختياري)
├── 📄 versions.tf      # إصدارات Terraform (اختياري)
└── 📄 README.md        # التوثيق
مثال عملي: إنشاء Module لـ EC2
1️⃣ هيكل المشروع
text

📁 terraform-project/
├── 📄 main.tf              # Root Module
├── 📄 variables.tf
├── 📄 outputs.tf
│
└── 📁 modules/
    └── 📁 ec2-instance/    # Child Module
        ├── 📄 main.tf
        ├── 📄 variables.tf
        └── 📄 outputs.tf
2️⃣ إنشاء الـ Module
hcl

# modules/ec2-instance/variables.tf
# ─────────────────────────────────

variable "instance_name" {
  description = "اسم الـ Instance"
  type        = string
}

variable "instance_type" {
  description = "نوع الـ Instance"
  type        = string
  default     = "t2.micro"
}

variable "ami_id" {
  description = "معرف الـ AMI"
  type        = string
}

variable "environment" {
  description = "البيئة (dev, staging, prod)"
  type        = string
  default     = "dev"
}
hcl

# modules/ec2-instance/main.tf
# ────────────────────────────

resource "aws_instance" "this" {
  ami           = var.ami_id
  instance_type = var.instance_type

  tags = {
    Name        = var.instance_name
    Environment = var.environment
    ManagedBy   = "Terraform"
  }
}

resource "aws_eip" "this" {
  instance = aws_instance.this.id
  domain   = "vpc"

  tags = {
    Name = "${var.instance_name}-eip"
  }
}
hcl

# modules/ec2-instance/outputs.tf
# ───────────────────────────────

output "instance_id" {
  description = "معرف الـ Instance"
  value       = aws_instance.this.id
}

output "public_ip" {
  description = "عنوان IP العام"
  value       = aws_eip.this.public_ip
}

output "private_ip" {
  description = "عنوان IP الخاص"
  value       = aws_instance.this.private_ip
}
3️⃣ استخدام الـ Module
hcl

# main.tf (Root Module)
# ─────────────────────

provider "aws" {
  region = "us-east-1"
}

# استدعاء Module لإنشاء Web Server
module "web_server" {
  source = "./modules/ec2-instance"

  instance_name = "web-server"
  instance_type = "t2.small"
  ami_id        = "ami-0c55b159cbfafe1f0"
  environment   = "production"
}

# استدعاء نفس Module لإنشاء Database Server
module "db_server" {
  source = "./modules/ec2-instance"

  instance_name = "database-server"
  instance_type = "t2.medium"
  ami_id        = "ami-0c55b159cbfafe1f0"
  environment   = "production"
}

# استخدام المخرجات
output "web_server_ip" {
  value = module.web_server.public_ip
}

output "db_server_ip" {
  value = module.db_server.private_ip
}
مصادر الـ Modules
hcl

# 1️⃣ مسار محلي (Local Path)
module "local_module" {
  source = "./modules/my-module"
}

# 2️⃣ Terraform Registry
module "vpc" {
  source  = "terraform-aws-modules/vpc/aws"
  version = "5.0.0"
}

# 3️⃣ GitHub
module "github_module" {
  source = "github.com/username/repo//modules/example"
}

# 4️⃣ Git URL
module "git_module" {
  source = "git::https://example.com/repo.git?ref=v1.0.0"
}

# 5️⃣ S3 Bucket
module "s3_module" {
  source = "s3::https://s3-eu-west-1.amazonaws.com/bucket/module.zip"
}
text

┌─────────────────────────────────────────────────────────────┐
│                    مصادر Modules                            │
│                                                             │
│   Local Path ──────────▶ ./modules/name                    │
│                                                             │
│   Registry ────────────▶ namespace/name/provider           │
│                                                             │
│   GitHub ──────────────▶ github.com/user/repo              │
│                                                             │
│   Bitbucket ───────────▶ bitbucket.org/user/repo           │
│                                                             │
│   Generic Git ─────────▶ git::url                          │
│                                                             │
│   HTTP URLs ───────────▶ https://example.com/module.zip    │
│                                                             │
│   S3 Bucket ───────────▶ s3::https://bucket/path           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
مثال متقدم: Module مع Count و For_each
hcl

# إنشاء عدة instances باستخدام count
module "web_servers" {
  source   = "./modules/ec2-instance"
  count    = 3

  instance_name = "web-server-${count.index + 1}"
  instance_type = "t2.micro"
  ami_id        = "ami-0c55b159cbfafe1f0"
  environment   = "production"
}

# إنشاء instances مختلفة باستخدام for_each
module "app_servers" {
  source   = "./modules/ec2-instance"
  for_each = {
    api     = "t2.small"
    worker  = "t2.medium"
    cache   = "t2.micro"
  }

  instance_name = "${each.key}-server"
  instance_type = each.value
  ami_id        = "ami-0c55b159cbfafe1f0"
  environment   = "production"
}

# الوصول للمخرجات
output "web_ips" {
  value = module.web_servers[*].public_ip
}

output "app_ips" {
  value = { for k, v in module.app_servers : k => v.public_ip }
}
تمرير Providers للـ Module
hcl

# تعريف عدة providers
provider "aws" {
  alias  = "us_east"
  region = "us-east-1"
}

provider "aws" {
  alias  = "eu_west"
  region = "eu-west-1"
}

# تمرير provider معين للـ module
module "us_infrastructure" {
  source = "./modules/infrastructure"
  
  providers = {
    aws = aws.us_east
  }
}

module "eu_infrastructure" {
  source = "./modules/infrastructure"
  
  providers = {
    aws = aws.eu_west
  }
}
أفضل الممارسات
text

┌─────────────────────────────────────────────────────────────┐
│                   ✅ أفضل الممارسات                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 📝 التوثيق الجيد                                        │
│     └── README.md لكل module                               │
│                                                             │
│  2. 🔢 تحديد الإصدارات                                      │
│     └── استخدم version constraints                         │
│                                                             │
│  3. 🎯 الغرض الواحد                                         │
│     └── كل module يحل مشكلة واحدة                          │
│                                                             │
│  4. 📤 المخرجات المفيدة                                     │
│     └── أخرج كل ما قد يحتاجه المستخدم                       │
│                                                             │
│  5. ✔️ التحقق من المدخلات                                   │
│     └── استخدم validation blocks                           │
│                                                             │
│  6. 🏷️ التسمية الواضحة                                     │
│     └── أسماء وصفية للمتغيرات                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
hcl

# مثال على التحقق من المدخلات
variable "environment" {
  description = "البيئة"
  type        = string

  validation {
    condition     = contains(["dev", "staging", "prod"], var.environment)
    error_message = "البيئة يجب أن تكون: dev, staging, أو prod"
  }
}

variable "instance_type" {
  description = "نوع الـ Instance"
  type        = string

  validation {
    condition     = can(regex("^t[2-3]\\.", var.instance_type))
    error_message = "يجب استخدام instance type من نوع t2 أو t3"
  }
}
أوامر Terraform المتعلقة بـ Modules
Bash

# تحميل الـ modules
terraform init

# تحديث الـ modules
terraform init -upgrade

# عرض الـ modules المستخدمة
terraform providers

# عرض معلومات module معين
terraform state list module.web_server
ملخص
text

┌─────────────────────────────────────────────────────────────┐
│                        Module = 📦                          │
│                                                             │
│    Variables ──▶ Resources ──▶ Outputs                     │
│    (مدخلات)      (منطق)        (مخرجات)                     │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  module "name" {                                     │   │
│  │    source = "path/to/module"                        │   │
│  │    variable1 = value1                               │   │
│  │    variable2 = value2                               │   │
│  │  }                                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  الوصول للمخرجات: module.name.output_name                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘

✅ صح تماماً!
عندما تستدعي Module، بتبعتله قيم المتغيرات اللي هو محتاجها.

الفكرة ببساطة
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   Root Module (main.tf)          Child Module               │
│   ─────────────────────          ────────────               │
│                                                             │
│   module "web" {                 variable "name" { }        │
│     source = "./modules/ec2"    variable "type" { }        │
│                                                             │
│     name = "my-server"    ───▶   var.name                  │
│     type = "t2.micro"     ───▶   var.type                  │
│   }                                                         │
│                                                             │
│   أنت بتبعت القيم          ◀───   هو معرف المتغيرات        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
مثال عملي
📁 الـ Module (اللي بيستقبل)
hcl

# modules/ec2/variables.tf

variable "instance_name" {
  type = string
}

variable "instance_type" {
  type    = string
  default = "t2.micro"    # قيمة افتراضية
}

variable "environment" {
  type = string
}
hcl

# modules/ec2/main.tf

resource "aws_instance" "this" {
  ami           = "ami-12345"
  instance_type = var.instance_type    # بيستخدم المتغير

  tags = {
    Name = var.instance_name           # بيستخدم المتغير
    Env  = var.environment             # بيستخدم المتغير
  }
}
📄 استدعاء الـ Module (اللي بيبعت)
hcl

# main.tf

module "web_server" {
  source = "./modules/ec2"

  # هنا بتبعت قيم المتغيرات
  instance_name = "web-server-1"      # ◀── required
  instance_type = "t2.small"          # ◀── optional (فيه default)
  environment   = "production"        # ◀── required
}
⚠️ ملاحظات مهمة
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  1️⃣  المتغيرات اللي مفيهاش default = لازم تبعتها          │
│                                                             │
│  2️⃣  المتغيرات اللي فيها default = اختياري تبعتها         │
│                                                             │
│  3️⃣  اسم المتغير في الاستدعاء = نفس اسمه في الـ module    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
hcl

# مثال على الفرق

# في الـ Module
variable "name" {
  type = string
  # ❌ مفيش default = لازم تبعتها
}

variable "size" {
  type    = string
  default = "small"
  # ✅ فيه default = اختياري
}

# ─────────────────────────────────

# في الاستدعاء
module "example" {
  source = "./modules/example"

  name = "my-app"    # ✅ لازم
  # size             # 🔸 مش لازم، هياخد "small"
}
💡 تشبيه بسيط
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   Module = Function في البرمجة                              │
│                                                             │
│   ┌─────────────────────┐    ┌─────────────────────┐       │
│   │  Python             │    │  Terraform          │       │
│   ├─────────────────────┤    ├─────────────────────┤       │
│   │  def create_server( │    │  variable "name" {} │       │
│   │    name,            │    │  variable "size" {} │       │
│   │    size             │    │                     │       │
│   │  ):                 │    │  resource "..." {}  │       │
│   │    ...              │    │                     │       │
│   └─────────────────────┘    └─────────────────────┘       │
│                                                             │
│   create_server(              module "x" {                  │
│     name="web",                 source = "..."              │
│     size="large"                name = "web"                │
│   )                             size = "large"              │
│                               }                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘

📤 ملف Outputs في Terraform
ببساطة: ما هو Output؟
Output = القيم اللي الـ Module بيرجعها بعد ما يخلص شغله

text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   Module بيشتغل ──▶ بينشئ Resources ──▶ بيرجع Outputs      │
│                                                             │
│   مثال:                                                     │
│   ──────                                                    │
│   أنشئ EC2 ──▶ Instance اتعمل ──▶ رجعلي الـ IP بتاعه       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
🎯 ليه نستخدم Outputs؟
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  1️⃣  عرض معلومات مهمة بعد terraform apply                  │
│      └── زي IP Address أو DNS Name                         │
│                                                             │
│  2️⃣  تمرير قيم من Module لـ Module تاني                    │
│      └── زي إنك تاخد ID من module وتديه لـ module تاني     │
│                                                             │
│  3️⃣  استخدامها في Scripts خارجية                           │
│      └── terraform output -json                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
📝 مثال بسيط
بدون Module (في Root)
hcl

# main.tf
resource "aws_instance" "web" {
  ami           = "ami-12345"
  instance_type = "t2.micro"
}

# outputs.tf
output "server_ip" {
  description = "Public IP of the server"
  value       = aws_instance.web.public_ip
}

output "server_id" {
  description = "Instance ID"
  value       = aws_instance.web.id
}
Bash

# بعد terraform apply هتشوف:
Outputs:

server_ip = "54.123.45.67"
server_id = "i-0abc123def456"
🔗 مثال مع Module
الـ Module (اللي بيرجع outputs)
hcl

# modules/ec2/main.tf
resource "aws_instance" "this" {
  ami           = var.ami_id
  instance_type = var.instance_type
}

resource "aws_eip" "this" {
  instance = aws_instance.this.id
}
hcl

# modules/ec2/outputs.tf

output "instance_id" {
  description = "ID of the EC2 instance"
  value       = aws_instance.this.id
}

output "public_ip" {
  description = "Public IP address"
  value       = aws_eip.this.public_ip
}

output "private_ip" {
  description = "Private IP address"
  value       = aws_instance.this.private_ip
}
استخدام الـ Outputs
hcl

# main.tf

module "web_server" {
  source        = "./modules/ec2"
  ami_id        = "ami-12345"
  instance_type = "t2.micro"
}

module "database" {
  source        = "./modules/ec2"
  ami_id        = "ami-12345"
  instance_type = "t2.medium"
}

# ─────────────────────────────────────────────
# استخدام output من module في module تاني
# ─────────────────────────────────────────────

resource "aws_security_group_rule" "allow_web" {
  type        = "ingress"
  from_port   = 3306
  to_port     = 3306
  protocol    = "tcp"
  
  # هنا بستخدم output من module
  source_security_group_id = module.web_server.instance_id
}

# ─────────────────────────────────────────────
# عرض الـ outputs في الـ Root
# ─────────────────────────────────────────────

output "web_ip" {
  value = module.web_server.public_ip
}

output "db_ip" {
  value = module.database.private_ip
}
📊 الفرق بين Variables و Outputs
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   Variables (المدخلات)         Outputs (المخرجات)           │
│   ────────────────────         ─────────────────           │
│                                                             │
│   بتدخل للـ Module             بتخرج من الـ Module          │
│         ↓                            ↑                      │
│   ┌─────────────────────────────────────────┐              │
│   │                                         │              │
│   │   var.name ──▶ Resource ──▶ output.id  │              │
│   │                                         │              │
│   │              MODULE                     │              │
│   └─────────────────────────────────────────┘              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
hcl

# Variables = اللي بتبعته للـ Module
module "web" {
  source = "./modules/ec2"
  name   = "my-server"      # ◀── Variable (Input)
}

# Outputs = اللي بتاخده من الـ Module  
output "ip" {
  value = module.web.public_ip   # ◀── Output
}
🔧 خصائص Output
hcl

output "connection_string" {
  description = "Database connection string"    # وصف
  value       = "mysql://${aws_db.main.endpoint}"  # القيمة
  sensitive   = true    # إخفاء القيمة (للبيانات الحساسة)
}

output "instance_ids" {
  description = "List of instance IDs"
  value       = aws_instance.web[*].id
  
  # يعتمد على resource معين
  depends_on = [aws_instance.web]
}
💻 أوامر مفيدة
Bash

# عرض كل الـ outputs
terraform output

# عرض output معين
terraform output server_ip

# عرض بصيغة JSON
terraform output -json

# عرض قيمة بدون quotes
terraform output -raw server_ip
📌 ملخص
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    🔄 الدورة الكاملة                        │
│                                                             │
│   ┌──────────┐      ┌──────────┐      ┌──────────┐        │
│   │Variables │ ───▶ │ Module   │ ───▶ │ Outputs  │        │
│   │ (Input)  │      │ (Logic)  │      │ (Return) │        │
│   └──────────┘      └──────────┘      └──────────┘        │
│                                                             │
│   تبعتله           يشتغل             يرجعلك               │
│   المتغيرات        وينشئ             النتيجة               │
│                    Resources                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘

🔐 Backend و Credentials في Terraform
1️⃣ ما هو Backend؟
Backend = المكان اللي Terraform بيخزن فيه ملف الـ State

text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   terraform apply                                           │
│         │                                                   │
│         ▼                                                   │
│   ┌─────────────┐         ┌─────────────────────┐         │
│   │   Resources │ ──────▶ │  terraform.tfstate  │         │
│   │   Created   │         │     (State File)    │         │
│   └─────────────┘         └─────────────────────┘         │
│                                    │                        │
│                                    ▼                        │
│                           ┌───────────────┐                │
│                           │   Backend     │                │
│                           │  (Storage)    │                │
│                           └───────────────┘                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
📦 أنواع Backend
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   1️⃣ Local Backend (الافتراضي)                              │
│      └── الـ State بيتخزن على جهازك                         │
│      └── ملف: terraform.tfstate                            │
│                                                             │
│   2️⃣ Remote Backend                                         │
│      ├── S3 (AWS)                                          │
│      ├── Azure Blob Storage                                │
│      ├── Google Cloud Storage (GCS)                        │
│      ├── Terraform Cloud                                   │
│      ├── HashiCorp Consul                                  │
│      └── PostgreSQL, HTTP, etc.                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
ليه نستخدم Remote Backend؟
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   ❌ بدون Remote Backend          ✅ مع Remote Backend      │
│   ─────────────────────           ──────────────────       │
│                                                             │
│   • State على جهازك بس           • State مشترك للفريق      │
│   • ممكن يضيع                    • محفوظ وآمن              │
│   • تعارض لو فريق                • State Locking           │
│   • مفيش تاريخ                   • Versioning              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
🔧 إعداد S3 Backend (الأشهر)
الخطوة 1: إنشاء S3 Bucket و DynamoDB Table
hcl

# backend-setup/main.tf
# هذا الملف تشغله مرة واحدة فقط لإنشاء البنية التحتية للـ Backend

provider "aws" {
  region = "us-east-1"
}

# S3 Bucket لتخزين State
resource "aws_s3_bucket" "terraform_state" {
  bucket = "my-terraform-state-bucket-12345"  # لازم يكون unique

  tags = {
    Name = "Terraform State"
  }
}

# تفعيل Versioning
resource "aws_s3_bucket_versioning" "enabled" {
  bucket = aws_s3_bucket.terraform_state.id
  versioning_configuration {
    status = "Enabled"
  }
}

# تفعيل Encryption
resource "aws_s3_bucket_server_side_encryption_configuration" "default" {
  bucket = aws_s3_bucket.terraform_state.id

  rule {
    apply_server_side_encryption_by_default {
      sse_algorithm = "AES256"
    }
  }
}

# منع Public Access
resource "aws_s3_bucket_public_access_block" "public_access" {
  bucket                  = aws_s3_bucket.terraform_state.id
  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

# DynamoDB Table للـ State Locking
resource "aws_dynamodb_table" "terraform_locks" {
  name         = "terraform-state-locks"
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "LockID"

  attribute {
    name = "LockID"
    type = "S"
  }

  tags = {
    Name = "Terraform State Locks"
  }
}

output "s3_bucket_name" {
  value = aws_s3_bucket.terraform_state.id
}

output "dynamodb_table_name" {
  value = aws_dynamodb_table.terraform_locks.name
}
الخطوة 2: استخدام الـ Backend في مشروعك
hcl

# versions.tf

terraform {
  required_version = ">= 1.0.0"

  # ─────────────────────────────────────────
  # Backend Configuration
  # ─────────────────────────────────────────
  backend "s3" {
    bucket         = "my-terraform-state-bucket-12345"
    key            = "project-name/terraform.tfstate"
    region         = "us-east-1"
    
    # State Locking
    dynamodb_table = "terraform-state-locks"
    
    # Encryption
    encrypt        = true
  }

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}
🔄 State Locking - كيف يعمل؟
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   Developer A                      Developer B              │
│       │                                │                    │
│       ▼                                ▼                    │
│   terraform apply                  terraform apply          │
│       │                                │                    │
│       ▼                                ▼                    │
│   🔒 Lock State ✅                 🔒 Lock State ❌          │
│       │                                │                    │
│       ▼                                ▼                    │
│   Working...                       ⏳ Waiting...            │
│       │                                │                    │
│       ▼                                │                    │
│   🔓 Unlock                            │                    │
│                                        ▼                    │
│                                   🔒 Lock State ✅          │
│                                        │                    │
│                                        ▼                    │
│                                   Working...                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
2️⃣ Credentials - بيانات الاعتماد
طرق إدخال AWS Credentials
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   🔐 طرق الـ Credentials (مرتبة من الأفضل للأسوأ)          │
│                                                             │
│   1️⃣ IAM Role (للـ EC2/ECS)           ⭐⭐⭐⭐⭐              │
│   2️⃣ Environment Variables            ⭐⭐⭐⭐               │
│   3️⃣ Shared Credentials File          ⭐⭐⭐⭐               │
│   4️⃣ AWS CLI Profile                  ⭐⭐⭐                │
│   5️⃣ في ملف Provider                  ⭐ (خطر!)            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
1️⃣ Environment Variables (الأكثر استخداماً)
Bash

# Linux/Mac
export AWS_ACCESS_KEY_ID="AKIAIOSFODNN7EXAMPLE"
export AWS_SECRET_ACCESS_KEY="wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
export AWS_DEFAULT_REGION="us-east-1"

# Windows (PowerShell)
$env:AWS_ACCESS_KEY_ID="AKIAIOSFODNN7EXAMPLE"
$env:AWS_SECRET_ACCESS_KEY="wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
$env:AWS_DEFAULT_REGION="us-east-1"

# Windows (CMD)
set AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
set AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
set AWS_DEFAULT_REGION=us-east-1
hcl

# providers.tf
# Terraform هياخد الـ credentials من Environment Variables تلقائياً

provider "aws" {
  region = "us-east-1"
  # مش محتاج تكتب credentials هنا!
}
2️⃣ Shared Credentials File
Bash

# الملف موجود في:
# Linux/Mac: ~/.aws/credentials
# Windows: C:\Users\USERNAME\.aws\credentials
ini

# ~/.aws/credentials

[default]
aws_access_key_id = AKIAIOSFODNN7EXAMPLE
aws_secret_access_key = wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

[production]
aws_access_key_id = AKIAI44QH8DHBEXAMPLE
aws_secret_access_key = je7MtGbClwBF/2Zp9Utk/h3yCo8nvbEXAMPLEKEY

[development]
aws_access_key_id = AKIAIOSFODNN7EXAMPLE
aws_secret_access_key = wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
hcl

# providers.tf
# استخدام profile معين

provider "aws" {
  region  = "us-east-1"
  profile = "production"    # بيستخدم credentials من [production]
}
3️⃣ AWS CLI Configuration
Bash

# إعداد AWS CLI
aws configure

# هيسألك:
# AWS Access Key ID [None]: AKIAIOSFODNN7EXAMPLE
# AWS Secret Access Key [None]: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
# Default region name [None]: us-east-1
# Default output format [None]: json

# إعداد profile مختلف
aws configure --profile production
hcl

# providers.tf

provider "aws" {
  region  = "us-east-1"
  profile = "production"
}
4️⃣ IAM Role (للـ EC2 - الأفضل للـ Production)
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   EC2 Instance                                              │
│   ┌─────────────────────────────────────┐                  │
│   │                                     │                  │
│   │   Terraform ───▶ Uses IAM Role     │                  │
│   │                        │            │                  │
│   │                        ▼            │                  │
│   │              Automatic Credentials  │                  │
│   │                                     │                  │
│   └─────────────────────────────────────┘                  │
│                        │                                    │
│                        ▼                                    │
│              No Keys to Manage! ✅                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
hcl

# providers.tf
# على EC2 مع IAM Role - مش محتاج أي credentials!

provider "aws" {
  region = "us-east-1"
  # Terraform هياخد credentials من IAM Role تلقائياً
}
⚠️ 5️⃣ في ملف Provider (خطر - لا تستخدمها!)
hcl

# ❌❌❌ لا تفعل هذا أبداً! ❌❌❌

provider "aws" {
  region     = "us-east-1"
  access_key = "AKIAIOSFODNN7EXAMPLE"          # ❌ خطر!
  secret_key = "wJalrXUtnFEMI/K7MDENG/bPxRfi"  # ❌ خطر!
}

# ليه خطر؟
# - ممكن ترفع الملف على GitHub بالغلط
# - أي حد يشوف الكود يشوف credentials
# - صعب تغييرها
🛡️ الطريقة الآمنة الكاملة
هيكل المشروع
text

terraform-project/
├── main.tf
├── variables.tf
├── outputs.tf
├── providers.tf
├── versions.tf
├── terraform.tfvars      # ⚠️ أضفه في .gitignore
└── .gitignore
ملف .gitignore
gitignore

# .gitignore

# Credentials - مهم جداً!
*.tfvars
terraform.tfvars
secrets.auto.tfvars

# State files
*.tfstate
*.tfstate.*
.terraform/

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Lock file (optional)
.terraform.lock.hcl
الملفات
hcl

# providers.tf

provider "aws" {
  region = var.aws_region
  # Credentials من Environment Variables أو AWS CLI
}
hcl

# variables.tf

variable "aws_region" {
  description = "AWS Region"
  type        = string
  default     = "us-east-1"
}
🔒 استخدام Secrets Manager أو Vault
hcl

# قراءة credentials من AWS Secrets Manager

data "aws_secretsmanager_secret" "db_credentials" {
  name = "my-database-credentials"
}

data "aws_secretsmanager_secret_version" "db_credentials" {
  secret_id = data.aws_secretsmanager_secret.db_credentials.id
}

locals {
  db_creds = jsondecode(data.aws_secretsmanager_secret_version.db_credentials.secret_string)
}

# استخدام الـ credentials
resource "aws_db_instance" "main" {
  # ...
  username = local.db_creds.username
  password = local.db_creds.password
}
📊 ملخص شامل
text

┌─────────────────────────────────────────────────────────────┐
│                    Backend & Credentials                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   🗄️ BACKEND                                                │
│   ─────────                                                 │
│   • يخزن State File                                        │
│   • استخدم S3 + DynamoDB للفريق                            │
│   • يوفر Locking و Versioning                              │
│                                                             │
│   terraform {                                               │
│     backend "s3" {                                         │
│       bucket         = "my-state-bucket"                   │
│       key            = "project/terraform.tfstate"         │
│       region         = "us-east-1"                         │
│       dynamodb_table = "terraform-locks"                   │
│       encrypt        = true                                │
│     }                                                      │
│   }                                                        │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   🔐 CREDENTIALS                                            │
│   ─────────────                                             │
│                                                             │
│   ✅ الأفضل: Environment Variables                         │
│      export AWS_ACCESS_KEY_ID="..."                        │
│      export AWS_SECRET_ACCESS_KEY="..."                    │
│                                                             │
│   ✅ أو: AWS CLI Profile                                   │
│      aws configure --profile myprofile                     │
│      provider "aws" { profile = "myprofile" }              │
│                                                             │
│   ✅ الأمثل: IAM Role (على EC2)                            │
│      مش محتاج أي credentials!                               │
│                                                             │
│   ❌ أبداً: في الكود مباشرة                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
▶️ مثال عملي سريع
Bash

# 1. إعداد Credentials
export AWS_ACCESS_KEY_ID="your-access-key"
export AWS_SECRET_ACCESS_KEY="your-secret-key"
export AWS_DEFAULT_REGION="us-east-1"

# 2. تهيئة Terraform (هينزل Provider ويتصل بالـ Backend)
terraform init

# 3. تأكد إن الاتصال شغال
terraform plan

# 4. شوف الـ State
terraform state list

🎯 إجابة على سؤاليك
السؤال الأول: ليه S3 + DynamoDB؟
الفرق بينهم
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   S3 Bucket                    DynamoDB Table               │
│   ──────────                   ───────────────              │
│                                                             │
│   📦 تخزين الـ State           🔒 قفل الـ State             │
│      (Storage)                    (Locking)                 │
│                                                             │
│   يحفظ ملف                     يمنع شخصين                  │
│   terraform.tfstate            يشتغلوا في                  │
│                                نفس الوقت                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
المشكلة بدون DynamoDB
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   ❌ بدون Locking (S3 فقط)                                  │
│                                                             │
│   Developer A              Developer B                      │
│       │                        │                            │
│       ▼                        ▼                            │
│   terraform apply          terraform apply                  │
│       │                        │                            │
│       ▼                        ▼                            │
│   Read State ◄────────────► Read State                     │
│   (Same Version)           (Same Version)                   │
│       │                        │                            │
│       ▼                        ▼                            │
│   Create EC2               Create EC2                       │
│       │                        │                            │
│       ▼                        ▼                            │
│   Write State              Write State                      │
│       │                        │                            │
│       └────────┬───────────────┘                           │
│                ▼                                            │
│         💥 CONFLICT!                                        │
│         State Corrupted                                     │
│         أو Resource ضاع                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
الحل مع DynamoDB
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   ✅ مع Locking (S3 + DynamoDB)                             │
│                                                             │
│   Developer A              Developer B                      │
│       │                        │                            │
│       ▼                        ▼                            │
│   terraform apply          terraform apply                  │
│       │                        │                            │
│       ▼                        ▼                            │
│   🔒 Lock (DynamoDB)       🔒 Try Lock                      │
│      ✅ Success                ❌ Failed                    │
│       │                        │                            │
│       ▼                        ▼                            │
│   Working...               ⏳ Waiting...                    │
│       │                    "State locked by A"              │
│       ▼                        │                            │
│   🔓 Unlock                    │                            │
│                                ▼                            │
│                           🔒 Lock ✅                        │
│                                │                            │
│                                ▼                            │
│                           Working...                        │
│                                                             │
│   ✅ No Conflicts!                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
ملخص
text

┌──────────────────┬─────────────────────────────────────────┐
│     Service      │              الوظيفة                    │
├──────────────────┼─────────────────────────────────────────┤
│   S3 Bucket      │  📦 تخزين ملف terraform.tfstate        │
│                  │  📜 Versioning (تاريخ التغييرات)        │
│                  │  🔐 Encryption (تشفير)                  │
├──────────────────┼─────────────────────────────────────────┤
│   DynamoDB       │  🔒 State Locking فقط!                  │
│                  │  يمنع terraform apply متزامن           │
└──────────────────┴─────────────────────────────────────────┘
السؤال الثاني: تخزين Credentials في JSON؟
✅ نعم ممكن تقنياً، لكن ⚠️ مش آمن!
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   ⚠️ تحذير: هذه الطريقة غير آمنة للـ Production!           │
│                                                             │
│   لكن للتعلم والتجربة المحلية... ممكن                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
الطريقة الأولى: ملف JSON
JSON

// credentials.json (أضفه في .gitignore!)
{
  "access_key": "AKIAIOSFODNN7EXAMPLE",
  "secret_key": "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY",
  "region": "us-east-1"
}
hcl

# main.tf

# قراءة الملف
locals {
  credentials = jsondecode(file("${path.module}/credentials.json"))
}

provider "aws" {
  region     = local.credentials.region
  access_key = local.credentials.access_key
  secret_key = local.credentials.secret_key
}
الطريقة الثانية: ملف tfvars
hcl

# credentials.tfvars (أضفه في .gitignore!)

aws_access_key = "AKIAIOSFODNN7EXAMPLE"
aws_secret_key = "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
aws_region     = "us-east-1"
hcl

# variables.tf

variable "aws_access_key" {
  description = "AWS Access Key"
  type        = string
  sensitive   = true    # يخفيها في الـ output
}

variable "aws_secret_key" {
  description = "AWS Secret Key"
  type        = string
  sensitive   = true
}

variable "aws_region" {
  description = "AWS Region"
  type        = string
}
hcl

# providers.tf

provider "aws" {
  region     = var.aws_region
  access_key = var.aws_access_key
  secret_key = var.aws_secret_key
}
Bash

# التشغيل
terraform plan -var-file="credentials.tfvars"
terraform apply -var-file="credentials.tfvars"
⚠️ ليه هذا خطر؟
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   ❌ مشاكل تخزين Credentials في ملفات:                     │
│                                                             │
│   1️⃣ ممكن ترفعها على GitHub بالغلط                         │
│      └── حتى لو حذفتها، تبقى في Git History!               │
│                                                             │
│   2️⃣ أي حد عنده access للجهاز يقدر يقرأها                  │
│                                                             │
│   3️⃣ الـ Credentials بتظهر في terraform plan               │
│      (حتى مع sensitive = true أحياناً)                     │
│                                                             │
│   4️⃣ صعب تغييرها وتوزيعها على الفريق                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
✅ الطريقة الصحيحة والآمنة
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   🏆 الترتيب من الأفضل للأسوأ:                              │
│                                                             │
│   1️⃣ IAM Role (على EC2/ECS)                                │
│      └── مفيش credentials أصلاً! ✨                         │
│                                                             │
│   2️⃣ Environment Variables                                 │
│      └── export AWS_ACCESS_KEY_ID="..."                    │
│      └── مش موجودة في أي ملف                               │
│                                                             │
│   3️⃣ AWS CLI Profile                                       │
│      └── aws configure                                     │
│      └── محفوظة في ~/.aws/credentials                      │
│      └── ملف محمي على مستوى النظام                         │
│                                                             │
│   4️⃣ Secrets Manager / Vault                               │
│      └── للـ Production environments                       │
│                                                             │
│   ❌ ملف JSON/tfvars في المشروع                             │
│      └── خطر جداً!                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
💡 نصيحتي
Bash

# الطريقة الأسهل والأأمن للتجربة المحلية:

# 1. نصب AWS CLI
# 2. اعمل configure
aws configure

# هيسألك:
# AWS Access Key ID: ........
# AWS Secret Access Key: ........
# Default region: us-east-1

# 3. في Terraform مش محتاج تكتب أي credentials!
hcl

# providers.tf
provider "aws" {
  region = "us-east-1"
  # خلاص! Terraform هياخد credentials من AWS CLI تلقائي
}
📊 ملخص نهائي
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   S3 + DynamoDB                                             │
│   ──────────────                                            │
│   S3       = 📦 Storage (تخزين State)                      │
│   DynamoDB = 🔒 Locking (منع التعارض)                      │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   Credentials                                               │
│   ───────────                                               │
│   ✅ Environment Variables أو AWS CLI = آمن                │
│   ⚠️ ملف JSON/tfvars = ممكن لكن خطر                        │
│   ❌ في الكود مباشرة = لا تفعل أبداً!                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘

🎯 إجابة مبسطة على سؤاليك
السؤال الأول: ليه S3 + DynamoDB؟
الجواب البسيط:
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   S3         =  📦 يخزن الملف                               │
│                                                             │
│   DynamoDB   =  🔒 يقفل الملف وقت الاستخدام                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
المشكلة بدون DynamoDB:
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   أحمد                              محمد                    │
│     │                                 │                     │
│     ▼                                 ▼                     │
│   terraform apply                 terraform apply           │
│     │                                 │                     │
│     ▼                                 ▼                     │
│   قرأ الـ State                   قرأ الـ State             │
│   (نفس النسخة)                    (نفس النسخة)              │
│     │                                 │                     │
│     ▼                                 ▼                     │
│   أنشأ Server                     أنشأ Server               │
│     │                                 │                     │
│     ▼                                 ▼                     │
│   حفظ State                       حفظ State                 │
│     │                                 │                     │
│     └─────────────┬───────────────────┘                     │
│                   ▼                                         │
│             💥 تعارض!                                       │
│          واحد من الـ Servers                                │
│             ضاع من الـ State!                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
الحل مع DynamoDB:
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   أحمد                              محمد                    │
│     │                                 │                     │
│     ▼                                 ▼                     │
│   terraform apply                 terraform apply           │
│     │                                 │                     │
│     ▼                                 ▼                     │
│   🔒 قفل الـ State ✅             🔒 حاول يقفل ❌            │
│     │                                 │                     │
│     ▼                                 ▼                     │
│   شغال...                         ⏳ مستني...               │
│     │                             "State مقفول"             │
│     ▼                                 │                     │
│   🔓 فتح القفل                        │                     │
│                                       ▼                     │
│                                  🔒 قفل ✅                  │
│                                       │                     │
│                                       ▼                     │
│                                   شغال...                   │
│                                                             │
│   ✅ مفيش تعارض!                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
باختصار شديد:
Service	الوظيفة
S3	📦 يخزن ملف terraform.tfstate
DynamoDB	🔒 يمنع شخصين يشتغلوا في نفس الوقت
السؤال الثاني: Credentials في ملف JSON؟
✅ نعم، ممكن تقنياً!
JSON

// secrets.json
{
  "access_key": "AKIA...",
  "secret_key": "wJalr..."
}
hcl

# main.tf

locals {
  creds = jsondecode(file("secrets.json"))
}

provider "aws" {
  region     = "us-east-1"
  access_key = local.creds.access_key
  secret_key = local.creds.secret_key
}
⚠️ لكن خطر! ليه؟
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   ❌ المشاكل:                                               │
│                                                             │
│   1. ممكن ترفعها على GitHub بالغلط!                        │
│      └── Keys هتتسرب                                       │
│                                                             │
│   2. أي حد يفتح المجلد يشوفها                               │
│                                                             │
│   3. لو نسيت تحطها في .gitignore = كارثة                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
✅ الطريقة الأفضل والأسهل:
Bash

# مرة واحدة بس:
aws configure

# ادخل:
# Access Key: AKIA...
# Secret Key: wJalr...
# Region: us-east-1
hcl

# providers.tf
provider "aws" {
  region = "us-east-1"
  # خلاص! مش محتاج تكتب أي credentials
  # Terraform هياخدها تلقائي من AWS CLI
}
📊 مقارنة الطرق:
text

┌──────────────────────┬────────────┬─────────────┐
│      الطريقة         │   سهولة   │    أمان     │
├──────────────────────┼────────────┼─────────────┤
│ AWS CLI (configure)  │    ✅✅    │    ✅✅✅   │
│ Environment Vars     │    ✅✅    │    ✅✅✅   │
│ ملف JSON            │    ✅✅    │    ❌❌     │
│ في الكود مباشرة      │    ✅      │    ❌❌❌   │
└──────────────────────┴────────────┴─────────────┘
💡 الخلاصة
text

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  🔹 S3 + DynamoDB:                                          │
│     S3 = تخزين    |    DynamoDB = قفل                      │
│                                                             │
│  🔹 Credentials:                                            │
│     استخدم: aws configure                                  │
│     أسهل وأأمن! 👍                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│                        الخلاصة                                 │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. Terraform = Infrastructure as Code                         │
│     - اكتب كود بدل ما تضغط يدوي                              │
│                                                                │
│  2. الأوامر الأساسية:                                         │
│     - init → plan → apply → destroy                           │
│                                                                │
│  3. التنظيم:                                                   │
│     - Modules للإعادة الاستخدام                               │
│     - Environments للفصل                                       │
│     - Remote State للفرق                                       │
│                                                                │
│  4. Variables:                                                 │
│     - variables.tf للتعريف                                    │
│     - terraform.tfvars للقيم                                  │
│                                                                │
│  5. Best Practices:                                            │
│     - استخدم version constraints                              │
│     - لا ترفع secrets                                         │
│     - استخدم CI/CD                                            │
│     - راجع Plan قبل Apply                                     │
│                                                                │
└────────────────────────────────────────────────────────────────┘
*************************************************************************
# ======================================
# أوامر Terraform الكاملة
# ======================================

# === Initialization ===
terraform init                    # تهيئة المشروع
terraform init -upgrade           # تحديث الـ Providers
terraform init -reconfigure       # إعادة تهيئة الـ Backend
terraform init -migrate-state     # نقل الـ State لـ Backend جديد

# === Planning ===
terraform plan                    # معاينة التغييرات
terraform plan -out=plan.tfplan   # حفظ الخطة
terraform plan -target=aws_instance.web  # هدف محدد
terraform plan -destroy           # معاينة الحذف

# === Applying ===
terraform apply                   # تنفيذ التغييرات
terraform apply plan.tfplan       # تنفيذ خطة محفوظة
terraform apply -auto-approve     # بدون تأكيد
terraform apply -target=aws_instance.web  # هدف محدد
terraform apply -replace=aws_instance.web # استبدال resource

# === Destroying ===
terraform destroy                 # حذف كل شيء
terraform destroy -target=aws_instance.web  # حذف محدد
terraform destroy -auto-approve   # بدون تأكيد

# === State Management ===
terraform state list              # عرض الـ Resources
terraform state show aws_instance.web  # تفاصيل resource
terraform state mv old_name new_name   # إعادة تسمية
terraform state rm aws_instance.web    # حذف من الـ State
terraform state pull              # سحب الـ State
terraform state push              # رفع الـ State

# === Import ===
terraform import aws_instance.web i-12345  # Import resource

# === Formatting & Validation ===
terraform fmt                     # تنسيق الكود
terraform fmt -recursive          # تنسيق كل الملفات
terraform fmt -check              # فحص بدون تعديل
terraform validate                # التحقق من الصحة

# === Output ===
terraform output                  # عرض المخرجات
terraform output vpc_id           # مخرج محدد
terraform output -json            # بصيغة JSON
terraform output -raw vpc_id      # القيمة فقط

# === Workspace ===
terraform workspace list          # عرض الـ Workspaces
terraform workspace new dev       # إنشاء جديد
terraform workspace select prod   # التبديل
terraform workspace delete dev    # حذف

# === Other ===
terraform version                 # عرض الإصدار
terraform providers               # عرض الـ Providers
terraform graph                   # رسم الـ Dependencies
terraform console                 # Console تفاعلي
terraform force-unlock LOCK_ID    # فتح الـ Lock